import * as React from 'react';
import Divider from 'material-ui/Divider';

const style = {
  marginTop: '15px'
};

const StyledDivider = props => <Divider {...props} style={style} />;

export default StyledDivider;